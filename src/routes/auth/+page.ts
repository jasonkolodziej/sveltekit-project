import type { PageLoad } from '../$types';
import type { PageData } from './$types';

// export const load: PageLoad = async ({ route, data, parent }) => {
// 	console.log(`${route.id}.PageLoad.from`);
// 	return data;
// };

export const prerender = 'auto';
